---
phase: 04-frontend-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/src/index.css
  - frontend/index.html
  - frontend/src/test-setup.ts
  - frontend/src/api/client.ts
  - frontend/src/hooks/useDarkMode.ts
  - frontend/src/hooks/useDarkMode.test.ts
  - frontend/src/utils/time.ts
  - frontend/src/utils/time.test.ts
  - frontend/src/utils/format.ts
  - frontend/src/utils/format.test.ts
  - frontend/src/components/panelColors.ts
  - frontend/src/components/PanelCard.tsx
  - frontend/src/components/PanelCard.test.tsx
  - frontend/src/components/SkeletonDashboard.tsx
  - frontend/src/components/SkeletonDashboard.test.tsx
  - frontend/src/components/EmptyState.tsx
  - frontend/src/components/EmptyState.test.tsx
autonomous: true
requirements: [UX-02, UX-03, UX-04]

must_haves:
  truths:
    - "Dark mode CSS variant is configured and useDarkMode hook toggles html.dark class"
    - "localStorage persists dark mode preference across page loads"
    - "No flash of wrong theme on page load (blocking script in index.html)"
    - "API client has typed getSummary, getAccounts, getBalanceHistory functions"
    - "PanelCard renders category label, formatted total balance, and account list"
    - "SkeletonDashboard renders animated placeholder cards matching panel layout"
    - "EmptyState shows setup prompt with link to Settings"
    - "timeAgo utility correctly formats relative time strings"
    - "Balance strings from API are formatted as USD currency with $ and commas"
  artifacts:
    - path: "frontend/src/hooks/useDarkMode.ts"
      provides: "Dark mode toggle hook with localStorage persistence"
      exports: ["useDarkMode"]
    - path: "frontend/src/api/client.ts"
      provides: "Typed API client for all dashboard endpoints"
      exports: ["getSummary", "getAccounts", "getBalanceHistory", "SummaryResponse", "AccountsResponse", "BalanceHistoryResponse"]
    - path: "frontend/src/components/PanelCard.tsx"
      provides: "Reusable panel card for liquid/savings/investments"
      exports: ["PanelCard"]
    - path: "frontend/src/components/SkeletonDashboard.tsx"
      provides: "Loading skeleton matching dashboard layout"
      exports: ["SkeletonDashboard"]
    - path: "frontend/src/components/EmptyState.tsx"
      provides: "First-launch setup prompt"
      exports: ["EmptyState"]
    - path: "frontend/src/components/panelColors.ts"
      provides: "Panel accent color constants"
      exports: ["PANEL_COLORS"]
    - path: "frontend/src/utils/time.ts"
      provides: "timeAgo utility extracted from Settings"
      exports: ["timeAgo"]
    - path: "frontend/src/utils/format.ts"
      provides: "Currency formatting utility"
      exports: ["formatCurrency"]
  key_links:
    - from: "frontend/src/hooks/useDarkMode.ts"
      to: "document.documentElement.classList"
      via: "useEffect adding/removing dark class"
      pattern: "classList\\.add\\('dark'\\)"
    - from: "frontend/src/index.css"
      to: "Tailwind dark mode"
      via: "@custom-variant dark directive"
      pattern: "@custom-variant dark"
    - from: "frontend/index.html"
      to: "localStorage theme"
      via: "blocking inline script"
      pattern: "localStorage\\.getItem\\('theme'\\)"
---

<objective>
Install recharts, set up dark mode infrastructure, extend the API client with typed dashboard endpoint functions, create reusable UI components (PanelCard, SkeletonDashboard, EmptyState), and extract shared utilities.

Purpose: Establish all foundational building blocks that Plans 02 and 03 depend on. After this plan, every component, hook, type, and utility needed by the dashboard page and charts exists and is independently tested.

Output: recharts installed, dark mode working, API client extended, 6 new components/utilities with tests, ResizeObserver test stub in place.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-frontend-dashboard/04-CONTEXT.md
@.planning/phases/04-frontend-dashboard/04-RESEARCH.md

@frontend/src/api/client.ts
@frontend/src/App.tsx
@frontend/src/index.css
@frontend/src/test-setup.ts
@frontend/index.html
@frontend/package.json
@frontend/src/pages/Settings.tsx
@frontend/vitest.config.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From frontend/src/api/client.ts (existing pattern):
```typescript
export async function checkAuth(): Promise<boolean>
export async function login(password: string): Promise<LoginResult>
export async function getSettings(): Promise<SettingsResponse>
export async function saveSettings(accessUrl: string): Promise<{ ok: boolean }>
export async function triggerSync(): Promise<{ ok: boolean }>
```

From Settings.tsx (timeAgo to extract):
```typescript
function timeAgo(dateStr: string): string {
  const seconds = Math.floor((Date.now() - new Date(dateStr).getTime()) / 1000)
  if (seconds < 60) return 'just now'
  if (seconds < 3600) return `${Math.floor(seconds / 60)}m ago`
  if (seconds < 86400) return `${Math.floor(seconds / 3600)}h ago`
  return `${Math.floor(seconds / 86400)}d ago`
}
```

From backend API contracts (Phase 3):
```
GET /api/summary → { liquid: "4230.50", savings: "15000.00", investments: "42000.00", last_synced_at: "2026-03-15T10:00:00Z" | null }
GET /api/accounts → { liquid: [{id, name, balance, account_type}], savings: [...], investments: [...], other: [...] }
GET /api/balance-history?days=30 → { liquid: [{date: "2026-03-01", balance: "4230.50"}], savings: [...], investments: [...] }
```
All balance values are JSON strings. last_synced_at is ISO 8601 or null.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install recharts, dark mode infrastructure, API client extensions, test setup</name>
  <files>
    frontend/package.json,
    frontend/src/index.css,
    frontend/index.html,
    frontend/src/test-setup.ts,
    frontend/src/api/client.ts,
    frontend/src/hooks/useDarkMode.ts,
    frontend/src/hooks/useDarkMode.test.ts,
    frontend/src/utils/time.ts,
    frontend/src/utils/time.test.ts,
    frontend/src/utils/format.ts,
    frontend/src/utils/format.test.ts
  </files>
  <behavior>
    - useDarkMode: toggle() switches isDark true/false
    - useDarkMode: when isDark=true, document.documentElement has class "dark"
    - useDarkMode: when isDark=false, document.documentElement does NOT have class "dark"
    - useDarkMode: localStorage "theme" is "dark" when isDark=true, "light" when isDark=false
    - useDarkMode: initial state reads from localStorage (default false when no stored value)
    - timeAgo("2026-03-15T10:00:00Z") with Date.now mocked to 30s later returns "just now"
    - timeAgo returns "Xm ago" for minutes, "Xh ago" for hours, "Xd ago" for days
    - formatCurrency("4230.50") returns "$4,230.50"
    - formatCurrency("0.00") returns "$0.00"
    - formatCurrency("15000") returns "$15,000.00"
  </behavior>
  <action>
    1. Install recharts: `cd frontend && npm install recharts`. If peer dep error, use `--legacy-peer-deps`. Document which was needed.

    2. Update `frontend/src/index.css` to add Tailwind v4 dark mode custom variant:
       ```css
       @import "tailwindcss";
       @custom-variant dark (&:where(.dark, .dark *));
       ```

    3. Update `frontend/index.html` — add blocking inline script in `<head>` BEFORE any other scripts to prevent dark mode flash:
       ```html
       <script>
         if (localStorage.getItem('theme') === 'dark') {
           document.documentElement.classList.add('dark')
         }
       </script>
       ```

    4. Update `frontend/src/test-setup.ts` — add ResizeObserver stub for Recharts compatibility:
       ```typescript
       import '@testing-library/jest-dom'

       global.ResizeObserver = class ResizeObserver {
         observe() {}
         unobserve() {}
         disconnect() {}
       }
       ```

    5. Create `frontend/src/hooks/useDarkMode.ts`:
       - useState initialized from `localStorage.getItem('theme') === 'dark'`
       - useEffect that adds/removes 'dark' class on `document.documentElement` and writes to localStorage
       - Returns `{ isDark: boolean, toggle: () => void }`
       - NO `window.matchMedia` / `prefers-color-scheme` detection (user decision: manual toggle only)

    6. Create `frontend/src/hooks/useDarkMode.test.ts`:
       - Test initial state when localStorage is empty (should be light)
       - Test initial state when localStorage has 'dark'
       - Test toggle from light to dark (check classList, localStorage)
       - Test toggle from dark to light

    7. Create `frontend/src/utils/time.ts` — extract `timeAgo` from Settings.tsx:
       - Copy the existing `timeAgo` function from `frontend/src/pages/Settings.tsx`
       - Export it as a named export
       - Update `Settings.tsx` to import from `../utils/time` instead of defining locally

    8. Create `frontend/src/utils/time.test.ts`:
       - Mock Date.now for deterministic testing
       - Test: <60s returns "just now"
       - Test: 120s returns "2m ago"
       - Test: 7200s returns "2h ago"
       - Test: 172800s returns "2d ago"

    9. Create `frontend/src/utils/format.ts`:
       - Export `formatCurrency(value: string): string` that parses string balance and formats as USD
       - Use `Number(value).toLocaleString('en-US', { style: 'currency', currency: 'USD' })`
       - Handle edge cases: "0.00" -> "$0.00", negative values for credit cards

    10. Create `frontend/src/utils/format.test.ts`:
        - Test: "4230.50" -> "$4,230.50"
        - Test: "0.00" -> "$0.00"
        - Test: "15000" -> "$15,000.00"
        - Test: "-500.25" -> "-$500.25"

    11. Extend `frontend/src/api/client.ts` with new typed functions (append, do not modify existing code):
        - Add interfaces: SummaryResponse, AccountItem, AccountsResponse, HistoryPoint, BalanceHistoryResponse
        - Add functions: getSummary(), getAccounts(), getBalanceHistory(days?: number)
        - Follow existing fetch pattern with `credentials: 'include'`
        - getSummary: GET /api/summary
        - getAccounts: GET /api/accounts
        - getBalanceHistory: GET /api/balance-history with optional ?days=N query param
  </action>
  <verify>
    <automated>cd /home/solomon/finance-visualizer/frontend && npx vitest run src/hooks/useDarkMode.test.ts src/utils/time.test.ts src/utils/format.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - recharts installed in package.json
    - index.css has @custom-variant dark directive
    - index.html has blocking dark mode script
    - test-setup.ts has ResizeObserver stub
    - useDarkMode hook works with 4+ passing tests
    - timeAgo extracted to utils with 4+ passing tests, Settings.tsx updated to import it
    - formatCurrency utility with 4+ passing tests
    - API client has typed getSummary, getAccounts, getBalanceHistory functions
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PanelCard, SkeletonDashboard, EmptyState components with panel colors</name>
  <files>
    frontend/src/components/panelColors.ts,
    frontend/src/components/PanelCard.tsx,
    frontend/src/components/PanelCard.test.tsx,
    frontend/src/components/SkeletonDashboard.tsx,
    frontend/src/components/SkeletonDashboard.test.tsx,
    frontend/src/components/EmptyState.tsx,
    frontend/src/components/EmptyState.test.tsx
  </files>
  <behavior>
    - PanelCard: renders panel label text (e.g. "Liquid")
    - PanelCard: renders formatted total balance (e.g. "$4,230.50")
    - PanelCard: renders each account name and balance in a list
    - PanelCard: uses accent color from PANEL_COLORS for visual distinction
    - PanelCard: includes dark mode classes (dark:bg-*, dark:text-*)
    - SkeletonDashboard: renders 3 animated placeholder cards
    - SkeletonDashboard: each card has animate-pulse class
    - EmptyState: renders "Connect your accounts" message
    - EmptyState: renders a link/button navigating to Settings page
  </behavior>
  <action>
    1. Create `frontend/src/components/panelColors.ts`:
       ```typescript
       export const PANEL_COLORS = {
         liquid:      { accent: '#3b82f6', label: 'Liquid',      darkAccent: '#60a5fa' },
         savings:     { accent: '#22c55e', label: 'Savings',     darkAccent: '#4ade80' },
         investments: { accent: '#a855f7', label: 'Investments', darkAccent: '#c084fc' },
       } as const
       ```

    2. Create `frontend/src/components/PanelCard.tsx`:
       - Props: `{ panelKey: 'liquid' | 'savings' | 'investments'; total: string; accounts: Array<{ id: string; name: string; balance: string }> }`
       - Import PANEL_COLORS and formatCurrency from utils/format
       - Render: white card (dark:bg-gray-800) with rounded-xl, shadow-md
       - Top: colored accent bar or dot using PANEL_COLORS[panelKey].accent
       - Category label from PANEL_COLORS[panelKey].label in text-sm uppercase
       - Total balance formatted via formatCurrency in text-2xl font-bold
       - Account list: each account as a flex row with name (left) and balance (right)
       - Responsive: full width on mobile, expands to fill grid cell on desktop
       - Dark mode: dark:bg-gray-800, dark:text-gray-100, dark:border-gray-700

    3. Create `frontend/src/components/PanelCard.test.tsx`:
       - Test: renders label text matching PANEL_COLORS
       - Test: renders formatted total balance
       - Test: renders all account names
       - Test: renders all account balances formatted
       - Test: renders nothing problematic with empty accounts array

    4. Create `frontend/src/components/SkeletonDashboard.tsx`:
       - Renders a 3-column grid (matching dashboard layout) of skeleton cards
       - Each skeleton card: bg-white dark:bg-gray-800 rounded-xl shadow-md p-5 animate-pulse
       - Inside each: gray placeholder bars for label (w-1/3), balance (w-2/3), and 3 account lines
       - Also render skeleton chart area below the panel grid (single wide skeleton block)
       - Use shimmer animation: `bg-gradient-to-r from-gray-200 via-gray-100 to-gray-200 dark:from-gray-700 dark:via-gray-600 dark:to-gray-700 bg-[length:800px_100%] animate-[shimmer_1.5s_infinite]`
       - Add shimmer keyframe to index.css inside @theme block if using custom shimmer (or use animate-pulse as fallback — Claude's discretion on which looks better)

    5. Create `frontend/src/components/SkeletonDashboard.test.tsx`:
       - Test: renders 3 skeleton cards
       - Test: skeleton cards have animate-pulse or shimmer animation class

    6. Create `frontend/src/components/EmptyState.tsx`:
       - Props: none (or optional onNavigateSettings callback)
       - Renders centered card with:
         - Icon or illustration placeholder (simple SVG or emoji)
         - Heading: "Connect your accounts to get started"
         - Subtext: brief explanation
         - CTA button/link navigating to /settings
       - Use `<Link to="/settings">` from react-router-dom for navigation
       - Dark mode support in all classes

    7. Create `frontend/src/components/EmptyState.test.tsx`:
       - Test: renders "Connect your accounts" text
       - Test: renders link to /settings
  </action>
  <verify>
    <automated>cd /home/solomon/finance-visualizer/frontend && npx vitest run src/components/PanelCard.test.tsx src/components/SkeletonDashboard.test.tsx src/components/EmptyState.test.tsx --reporter=verbose</automated>
  </verify>
  <done>
    - PANEL_COLORS constant exported with blue/green/purple accent colors
    - PanelCard renders label, total, and account list with dark mode support — 4+ tests pass
    - SkeletonDashboard renders 3 animated placeholder cards — 2+ tests pass
    - EmptyState renders setup prompt with Settings link — 2+ tests pass
    - All components use Tailwind dark: classes for dark mode compatibility
  </done>
</task>

</tasks>

<verification>
```bash
cd /home/solomon/finance-visualizer/frontend && npx vitest run --reporter=verbose
```
All existing tests still pass plus all new tests for useDarkMode, timeAgo, formatCurrency, PanelCard, SkeletonDashboard, EmptyState.
</verification>

<success_criteria>
- recharts is installed and importable
- Dark mode toggle works: useDarkMode hook toggles html.dark class and persists to localStorage
- No dark mode flash on page load (blocking script in index.html head)
- API client exports typed getSummary, getAccounts, getBalanceHistory
- PanelCard, SkeletonDashboard, EmptyState all render correctly with dark mode support
- timeAgo and formatCurrency utilities extracted and tested
- ResizeObserver stub in test-setup.ts for Recharts testing
- All tests pass: `cd frontend && npx vitest run`
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-dashboard/04-01-SUMMARY.md`
</output>
